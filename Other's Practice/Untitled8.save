#include<stdio.h>
#define N 3;
int main()
{
int a[N],i, pos=0, neg=0,zero=0;
printf("Enter the 3 values:\n");

for(i=0; i<N; i++)
scanf("%d",&a[i]);
for(i=0; i<N; i++)
{
if(a[i]>0)
pos++;

else if(a[i]<0)
neg++;
else
zero++;
}
printf("positive numbers: %d"pos);
printf("negative numbers: %d"neg);
printf("zeros numbers: %d"zero);

return 0;
}